# Architecture

Lyra is a declarative agent package. OpenSpine is the authority-bearing runtime.

The package may describe agents, routes, workflows, designed tools, memory scope,
and output channels. None of those declarations bypass the kernel's composition
and gate checks. The effective task grant remains the sole execution authority.
