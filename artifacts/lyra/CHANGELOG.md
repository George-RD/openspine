# Lyra changelog

## 1

- Formalized Lyra as OpenSpine's default owner-facing agent package.
- Declared the existing main assistant, workers, routes, policies, personality,
  and memory boundaries as one package.
- Completed the manifest's artifact declaration so it lists every typed family
  the kernel loads from this directory, including the terminal assistant, CLI
  and email routes, workflows, packs, templates, and golden sets.
