# Tasks

- [x] Add `persona` artifact kind: `PersonaElement` schema + registry field + loader block.
- [x] Seed nine Donna×Leo / digest-brief elements at kernel bootstrap as overlay learned artifacts with `ProducedBy` provenance, durable publication, atomic row/receipt persistence, restart repair, and fail-closed digest checks.
- [x] Add ten deterministic anti-pattern probes (AD-081/AD-083) and consume them in the model-swap golden-set output evaluator.
- [x] Trace every spec scenario to a named test: seed load/provenance → `seed_if_missing_loads_overlay_artifacts_with_real_traceable_provenance`; restart and second boot → `seed_survives_fresh_store_restart_without_duplicate_rows`; base exclusion → `generic_overlay_loader_excludes_persona_and_base_loader_rejects_fixture`; crash before row → `durable_persona_file_without_row_is_reconciled_on_retry`; exact ten probes → `registered_probe_registry_has_exactly_ten_unique_patterns`; violating sample → `every_anti_pattern_has_a_failing_sample`; clean output → `clean_output_trials_no_probes`; missing/corrupt file repair → `a_committed_row_with_a_missing_file_self_heals_without_new_provenance` and `a_corrupt_existing_file_is_repaired_to_the_row_digest`; atomic row/receipt → `audit_failure_rolls_back_learned_row_and_seeded_receipt`; proposal/nomination authority rejection → `artifact_propose_rejects_persona_kind` and `nomination_rejects_persona_kind_before_authority_path`; probe-only guidance → `seed_guidance_keeps_negative_constraints_in_probes_only`; digest presentation default → `digest_brief_default_is_a_learnable_presentation_only_guidance`. The dangling-event invariant is additionally covered by `persona_with_dangling_source_event_is_excluded_even_with_exchange_blob` and `dangling_seed_row_is_quarantined_and_reseeded`.
- [x] Keep the digest format as a default-only overlay; `implement-reflection-miner` owns the deferred owner-correction→miner→proposal route rather than this change claiming it.
- [x] Run cargo fmt, clippy --workspace -D warnings, cargo test --workspace, check-file-sizes.sh, claims, OMP ceremony, and strict OpenSpec validation through `./scripts/check.sh implement-personality-seed`.
