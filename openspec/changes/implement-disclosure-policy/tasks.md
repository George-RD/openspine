## 1. Schema and deterministic gate
- [x] Add strict disclosure classes, policy keys, classified provenance, generalized queries, and deterministic policy coverage checks.
- [x] Prove private-context effects, provenance-based checks after generalization, covered egress, and uncovered owner-question output with named tests.

## 2. Persistence and standing-rule envelope
- [x] Add idempotent disclosure policy storage and migration wiring.
- [x] Persist owner answers with carve-outs, merge same-scope egress approvals, and persist one independent D-107 standing-rule envelope per (relationship, disclosure_class, egress_class) scope.
- [x] Cover policy recovery, two scopes on one egress, two egress classes on one scope, and protection of an existing real-egress standing rule.

## 3. Owner escalation and verification
- [x] Add typed owner-question escalation payload and kernel routing/audit handling.
- [x] Run the unmasked change gate.
