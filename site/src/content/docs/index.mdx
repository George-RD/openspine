---
title: Give your agent a backbone.
description: OpenSpine is a self-hostable runtime substrate for governed agents.
template: splash
hero:
  tagline: Most tools ask what your AI agent <em>can</em> do. OpenSpine asks what it's <em>allowed</em> to do — and proves it.
  actions:
    - text: Quickstart
      link: /openspine/quickstart/
      icon: right-arrow
    - text: Why OpenSpine
      link: /openspine/why-openspine/
      icon: right-arrow
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Three pillars

<CardGrid stagger>
	<Card title="Composed authority" icon="code-branch">
		An agent has no trust by default. The safety layer (substrate) combines all rules. It decides exactly what the agent is allowed to do. It issues a temporary permission for each task.
	</Card>
	<Card title="One gate" icon="forward-slash">
		The agent must pass through a single gateway for any action. This checks what the agent does in the real world (effectful action). We write every decision to a tamper-evident log (hash-chain audited).
	</Card>
	<Card title="Provable claims" icon="approve-check">
		We do not just say OpenSpine is safe. Every security claim maps to a real test. Our check script runs all tests on every change.
	</Card>
</CardGrid>
